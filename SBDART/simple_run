#!/bin/bash

root=sbchk_transmittance

# Remove existing output file
rm -f $root.transmittance

# Run SBDART with the provided input file
echo "Running SBDART for transmittance with height..."
./sbdart > $root.transmittance < INPUT

echo "SBDART run completed. Output saved to $root.transmittance"
